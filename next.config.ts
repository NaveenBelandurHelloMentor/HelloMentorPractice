import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
    domains: ['hellomentor.in', 's3-example-hello-mentor.s3.ap-south-1.amazonaws.com'],
  },
  /* config options here */
};

export default nextConfig;
